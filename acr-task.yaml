version: v1.0.0
steps:
  - cmd: "{{.Run.Registry}}/base-artifacts/azure-cli:2.0.75 ls"
    env:
      - ACR_NAME={{.Run.RegistryName}}
      - SP={{.Values.SP}}
      - PASSWORD={{.Values.PASSWORD}}
      - TENANT={{.Values.TENANT}}
