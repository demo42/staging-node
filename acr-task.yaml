version: v1.0.0
steps:
  - cmd: "orca run {{.Run.Registry}}/base-artifacts/azure-cli:2.0.75"
    entryPoint: "./import.sh"
    env:
      - ACR_NAME={{.Run.RegistryName}}
      - TENANT={{.Values.TENANT}}
      - SP={{.Values.SP}}
      - PASSWORD={{.Values.PASSWORD}}
