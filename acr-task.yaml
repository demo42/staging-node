version: v1.0.0
steps:
  - cmd: "docker run {{.Run.Registry}}/base-artifacts/azure-cli:2.0.75 ./import.sh"
    env:
      - ACR_NAME={{.Run.RegistryName}}
      - SP={{.Values.SP}}
      - PASSWORD={{.Values.PASSWORD}}
      - TENANT={{.Values.TENANT}}
